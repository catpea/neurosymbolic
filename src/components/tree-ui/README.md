# x-tree-ui

Collapsible tree viewer for nested operating-system structures.

- Tag: `x-tree-ui`
- Attributes: none
- Property: `tree`
- Behavior: renders nested nodes with Bootstrap `collapse` and exposes node-specific AI actions through robot buttons when `node.ai.actions` is present
