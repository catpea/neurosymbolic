# x-system-dashboard

Live operating-system dashboard for the browser shell.

- Tag: `x-system-dashboard`
- Attributes: none
- Behavior: reads `x-os.mcpSnapshot()`, renders overview cards, uses `x-tree-ui` for nested structures, and opens a context-sensitive AI drafting popup for robot-enabled tree nodes
